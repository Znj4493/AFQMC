# AFQMC语义相似度匹配项目 - 分阶段学习计划

## 🎯 计划核心亮点

本计划针对2026年AI求职市场精心设计，重点突出：

1. **跳过传统LSTM，直接学习Transformer架构** - 符合当前行业趋势
2. **使用MacBERT作为Baseline** - 中文任务的最佳选择之一
3. **LoRA微调大语言模型** - 掌握最热门的LLM微调技术
4. **显存优化技术全覆盖** - AMP、梯度累积、量化、梯度检查点
5. **对比实验设计** - MacBERT vs LLM，深入理解不同方案的优劣

---

## 项目概况

**任务类型**: 中文文本对的语义相似度二分类任务
**数据规模**: 训练集32,000条，测试集5,000条
**数据特点**:
- 标签分布不平衡（label 0: 69.1%, label 1: 30.9%）
- 领域：蚂蚁金服金融产品相关问题
- 格式：JSONL（每行一个JSON对象，包含text1, text2, label字段）

**硬件环境**: RTX 4060（8GB显存）

---

## 分阶段实施计划

### 阶段一：环境搭建与数据探索（1-2天）

**目标**:
- 搭建深度学习开发环境
- 了解NLP任务的基本流程
- 探索和理解数据集特征

**知识点**:
- PyTorch基础框架介绍
- Transformers库的作用
- JSONL数据格式
- 文本对匹配任务的本质

**实施步骤**:
1. 创建Python虚拟环境
2. 安装必要的库（PyTorch, Transformers, pandas, numpy等）
3. 编写数据加载脚本（`data_loader.py`）
4. 进行数据探索分析（EDA）：
   - 统计文本长度分布
   - 查看类别分布
   - 分析正负样本特征

**产出文件**:
- `requirements.txt`: 依赖库列表
- `data_loader.py`: 数据加载工具
- `eda.ipynb` 或 `eda.py`: 数据探索脚本

---

### 阶段二：MacBERT Baseline模型（2-3天）

**目标**:
- 直接上手Transformer架构
- 理解预训练模型的工作原理
- 建立高质量的Baseline
- 掌握显存优化技巧

**知识点**:
- Transformer架构基础
- BERT/MacBERT的工作原理
- Tokenizer的作用（WordPiece分词）
- 自动混合精度训练（AMP）
- 梯度累积技术
- 损失函数与优化器
- 评估指标（准确率、F1-score、AUC）

**实施步骤**:
1. 使用 `hfl/chinese-macbert-base` 构建文本对匹配模型
   - 输入格式：[CLS] text1 [SEP] text2 [SEP]
   - 使用[CLS]位置的输出进行分类
2. 编写训练脚本（`train.py`）
   - 集成AMP（自动混合精度）
   - 实现梯度累积（适应8GB显存）
   - 配置合适的超参数
3. 实现验证集切分和评估
4. 保存模型checkpoint

**显存优化配置**（针对RTX 4060 8GB）:
- Batch size: 16-32（配合梯度累积）
- 梯度累积步数: 2-4步
- 混合精度训练: FP16
- 最大序列长度: 128

**预期性能**: 准确率约78-83%

**产出文件**:
- `model_macbert.py`: MacBERT模型定义
- `train.py`: 训练主脚本（含AMP和梯度累积）
- `utils.py`: 工具函数（评估指标等）

---

### 阶段三：模型优化与进阶技巧（3-4天）

**目标**:
- 学习模型优化的常用技巧
- 处理类别不平衡问题
- 提升MacBERT模型性能

**知识点**:
- 对抗训练（FGM、PGM）提升鲁棒性
- 损失函数优化（Focal Loss、Label Smoothing）
- 数据增强技术（EDA、回译）
- 学习率warmup和衰减策略
- K-fold交叉验证
- 模型集成方法

**实施步骤**:
1. 实现针对不平衡数据的优化
   - 调整类别权重
   - 使用Focal Loss处理不平衡
2. 添加对抗训练提升鲁棒性（FGM）
3. 尝试不同的优化器和学习率策略
   - AdamW优化器
   - Warmup + Linear Decay
4. 实现简单的数据增强
5. （可选）K-fold交叉验证

**预期性能**: 准确率约83-88%

**产出文件**:
- `train_advanced.py`: 进阶训练脚本
- `adversarial.py`: 对抗训练实现
- `data_augmentation.py`: 数据增强工具

---

### 阶段四：大语言模型（LLM）微调 - LoRA实战（3-5天）

**目标**:
- 理解大语言模型的特点和优势
- 掌握LoRA高效微调技术
- 对比LLM与BERT的效果差异
- 学习当前求职市场最热门的技术

**知识点**:
- 大语言模型（LLM）基础概念
- LoRA（Low-Rank Adaptation）原理
- PEFT（Parameter-Efficient Fine-Tuning）技术
- 量化技术（Int4/Int8）降低显存需求
- QLoRA结合量化和LoRA
- BitsAndBytes库使用

**实施步骤**:
1. 选择轻量级大模型
   - 优先：`Qwen/Qwen-1.5-1.8B`（显存友好）
   - 备选：`Qwen/Qwen-1.5-7B-Chat-Int4`（量化版本）
2. 使用PEFT库实现LoRA微调
   - 配置LoRA参数（rank, alpha, dropout）
   - 设置目标模块（query, value层）
3. 编写LLM训练脚本
   - 使用4-bit量化（QLoRA）节省显存
   - 集成梯度检查点（gradient checkpointing）
   - Flash Attention 2加速（如果支持）
4. 对比实验：LLM vs MacBERT
   - 性能对比
   - 推理速度对比
   - 显存占用对比

**显存优化配置**（针对RTX 4060 8GB）:
- 模型量化: 4-bit（QLoRA）
- Batch size: 4-8
- 梯度累积: 4-8步
- 梯度检查点: 启用
- LoRA rank: 8-16
- 最大序列长度: 256-512

**预期性能**:
- Qwen-1.8B: 准确率约85-90%
- Qwen-7B-Int4: 准确率约88-92%

**产出文件**:
- `model_llm.py`: LLM模型定义
- `train_lora.py`: LoRA微调脚本
- `compare_models.py`: 模型对比分析脚本
- `实验对比报告.md`: LLM vs BERT详细对比

---

### 阶段五：推理与结果提交（1-2天）

**目标**:
- 完成测试集预测
- 生成提交文件
- 学习模型部署的基本概念

**知识点**:
- 模型推理优化
- Batch推理
- 结果后处理

**实施步骤**:
1. 编写推理脚本（`inference.py`）
2. 对测试集进行预测
3. 生成符合比赛要求的提交文件
4. 分析错误样本，理解模型局限性

**产出文件**:
- `inference.py`: 推理脚本
- `submission.csv` 或 `submission.jsonl`: 提交文件

---

### 阶段六：总结与优化（持续）

**目标**:
- 整理项目代码
- 撰写技术文档
- 准备面试材料

**实施步骤**:
1. 重构代码，提高可读性
2. 编写详细的README.md
3. 整理实验记录和结果对比
4. 准备项目答辩材料（如果有）
5. 总结学到的知识点，形成面试准备材料

**产出文件**:
- `README.md`: 项目说明文档
- `实验记录.md`: 各种方法的对比分析
- `面试准备.md`: 项目相关面试问题整理

---

## 关键文件结构（最终）

```
AFQMC/
├── dataset/
│   ├── train.jsonl          # 训练数据
│   └── test.jsonl           # 测试数据
├── src/
│   ├── data_loader.py       # 数据加载
│   ├── model_macbert.py     # MacBERT模型
│   ├── model_llm.py         # LLM模型（LoRA）
│   ├── train.py             # 训练脚本（含AMP和梯度累积）
│   ├── train_advanced.py    # 进阶训练（对抗训练等）
│   ├── train_lora.py        # LoRA微调脚本
│   ├── inference.py         # 推理脚本
│   ├── utils.py             # 工具函数
│   ├── adversarial.py       # 对抗训练
│   └── data_augmentation.py # 数据增强
├── config/
│   └── config.py            # 配置文件
├── checkpoints/             # 模型保存目录
│   ├── macbert/
│   └── qwen-lora/
├── results/                 # 结果保存目录
├── notebooks/
│   └── eda.ipynb           # 数据探索
├── requirements.txt         # 依赖库
├── 实验对比报告.md          # LLM vs BERT对比
└── README.md               # 项目说明

```

---

## 学习资源建议

1. **PyTorch基础**: PyTorch官方教程
2. **Transformer**: "Attention is All You Need" 论文
3. **BERT/MacBERT**: "BERT: Pre-training of Deep Bidirectional Transformers" 论文
4. **LoRA**: "LoRA: Low-Rank Adaptation of Large Language Models" 论文
5. **QLoRA**: "QLoRA: Efficient Finetuning of Quantized LLMs" 论文
6. **HuggingFace库**:
   - Transformers文档
   - PEFT库文档
   - BitsAndBytes库文档
7. **Qwen模型**: 阿里通义千问模型文档

---

## 时间估算

- **总计**: 约2-3周
- **每天投入**: 2-4小时
- **关键里程碑**:
  - 第1周末: 完成阶段一和阶段二（MacBERT Baseline，准确率78-83%）
  - 第2周中: 完成阶段三（模型优化，准确率83-88%）
  - 第2周末: 完成阶段四（LLM微调，准确率85-92%）
  - 第3周: 对比分析、提交和总结

---

## 成功标准

1. **技术目标**:
   - MacBERT模型准确率 > 80%
   - LLM模型（LoRA微调）准确率 > 85%
   - 完整的项目代码库（含MacBERT和LLM两条技术路线）
   - 可复现的实验结果
   - 详细的模型对比分析报告

2. **学习目标**:
   - 理解Transformer架构和BERT原理
   - 掌握PyTorch和HuggingFace Transformers库
   - 理解预训练模型微调流程
   - 掌握LoRA等高效微调技术
   - 理解显存优化技巧（AMP、梯度累积、量化等）
   - 能够独立完成NLP项目

3. **求职目标**:
   - 作为简历亮点项目展示
   - 能够详细讲解MacBERT和LLM两种方案
   - 掌握当前热门的LoRA微调技术
   - 准备好相关面试问题（Transformer、BERT、LoRA原理等）

---

## 核心技术栈

**基础框架**:
- PyTorch >= 2.0
- transformers >= 4.36.0
- datasets
- accelerate

**MacBERT相关**:
- hfl/chinese-macbert-base

**LLM微调相关**:
- peft (LoRA实现)
- bitsandbytes (量化)
- Qwen/Qwen-1.5-1.8B 或 Qwen/Qwen-1.5-7B-Chat

**优化工具**:
- torch.cuda.amp (自动混合精度)
- gradient_checkpointing (梯度检查点)
- flash-attention2 (可选，需GPU支持)

**数据处理与评估**:
- pandas
- numpy
- scikit-learn
- tqdm

---

## 下一步行动

**立即开始**: 阶段一 - 环境搭建与数据探索

具体任务：
1. 创建项目文件夹结构（src/, config/, checkpoints/, results/, notebooks/）
2. 创建Python虚拟环境（推荐使用conda或venv）
3. 编写 `requirements.txt`（包含上述核心技术栈）
4. 安装必要的库
5. 编写数据加载脚本（`data_loader.py`）并测试读取数据
6. 进行数据探索分析（EDA）

**预期产出**:
- 完整的开发环境
- 数据统计报告（文本长度分布、类别分布等）
- 对数据集的深入理解
