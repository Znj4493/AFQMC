# 基础深度学习框架
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers 和相关库
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.24.0
tokenizers>=0.15.0

# LLM 微调相关
peft>=0.7.0
bitsandbytes>=0.41.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# 机器学习工具
scikit-learn>=1.3.0

# 进度条和日志
tqdm>=4.65.0

# Jupyter 支持
jupyter>=1.0.0
ipykernel>=6.25.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 其他工具
sentencepiece>=0.1.99  # 某些模型的分词器需要
protobuf>=3.20.0       # 模型加载可能需要

# 可选：Flash Attention 2（需要编译，显著加速训练）
# flash-attn>=2.3.0  # 取消注释以安装，需要 CUDA 支持
